{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "installCommand": "if [ -d bot ]; then cd bot; fi && npm ci",
    "buildCommand": "if [ -d bot ]; then cd bot; fi && npm run build"
  },
  "deploy": {
    "startCommand": "if [ -d bot ]; then cd bot; fi && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

