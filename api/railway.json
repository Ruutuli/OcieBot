{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "installCommand": "if [ -d api ]; then cd api; fi && npm ci",
    "buildCommand": "if [ -d api ]; then cd api; fi && npm run build"
  },
  "deploy": {
    "startCommand": "if [ -d api ]; then cd api; fi && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

